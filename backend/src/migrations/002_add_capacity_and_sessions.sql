/*
# Migration 2: Add room capacity and session tracking

Adds: max_capacity column to rooms (0 = unlimited, 1+ = limit)
Creates: room_sessions table for tracking collaboration hours
Applied via Supabase MCP tool.
*/
